import React from "react";

const Home = () => {
    return (
        <div>
            <h2 className="text-center mt-3">Welcome, to the temporary chat application</h2>
        </div>
    );
};

export default Home;